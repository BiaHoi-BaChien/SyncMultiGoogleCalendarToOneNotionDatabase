# =============================================================
# アプリケーション全体で利用する環境変数
# それぞれの設定が何を意味するかをコメントで補足しています。
# 実際の運用環境では必要に応じて値を書き換えてください。
# =============================================================

# -------------------------------------------------------------
# Laravel アプリケーションの基本情報
# -------------------------------------------------------------
# アプリケーション名。通知やメールの差出人名にも利用されます。
APP_NAME=GoogleCalendarSyncToNotion
# 稼働環境を表す識別子。例: local / staging / production
APP_ENV=local
# アプリケーションの暗号化に使われるキー。"php artisan key:generate" で再発行できます。
APP_KEY=base64:iaB0lcrogSTlax3rOpfsqt6Vz9Jz8DrZKLBmrODuT4g=
# デバッグモードの ON/OFF。true にするとスタックトレースなどが表示されます。
APP_DEBUG=true
# アプリケーションのベース URL。CLI や通知のリンク生成に利用されます。
APP_URL=http://localhost

# -------------------------------------------------------------
# パスキー (WebAuthn) 認証に関する設定
# -------------------------------------------------------------
# パスキーの登録・認証時に利用する RP ID (通常はホスト名)
PASSKEY_RP_ID=localhost
# Web ブラウザから送信されるリクエストの正当性を検証するためのオリジン
PASSKEY_ORIGIN=http://localhost
# 認証チャレンジの有効期限 (秒)
PASSKEY_CHALLENGE_TTL=300

# -------------------------------------------------------------
# ログ出力の設定
# -------------------------------------------------------------
# 使用するログチャネル。daily では日次ローテーションでファイルが作成されます。
LOG_CHANNEL=daily
# 非推奨警告を出力するチャネル。null であれば無効。
LOG_DEPRECATIONS_CHANNEL=null
# ログの出力レベル。debug は最も詳細なログを出します。
LOG_LEVEL=debug

# -------------------------------------------------------------
# データベース接続情報 (MySQL)
# -------------------------------------------------------------
DB_CONNECTION=mysql
# データベースサーバーのホスト名または IP
DB_HOST=127.0.0.1
# データベースのポート番号
DB_PORT=3306
# 接続先データベース名
DB_DATABASE=laravel
# データベースユーザー名
DB_USERNAME=root
# データベースユーザーのパスワード
DB_PASSWORD=

# -------------------------------------------------------------
# キャッシュやキューなど Laravel サービスの設定
# -------------------------------------------------------------
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DRIVER=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
# セッションの有効期限 (分)
SESSION_LIFETIME=120

# -------------------------------------------------------------
# Memcached / Redis 設定
# -------------------------------------------------------------
MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# -------------------------------------------------------------
# メール送信設定
# -------------------------------------------------------------
MAIL_MAILER=smtp
# メールサーバーのホスト名
MAIL_HOST=
# メールサーバーのポート番号
MAIL_PORT=
# メール送信用のユーザー名
MAIL_USERNAME=
# メール送信用のパスワード
MAIL_PASSWORD=
# 暗号化方式 (ssl / tls など)
MAIL_ENCRYPTION=ssl
# 送信元メールアドレス
MAIL_FROM_ADDRESS=
# 送信元名。APP_NAME をそのまま利用できます。
MAIL_FROM_NAME="${APP_NAME}"

# -------------------------------------------------------------
# AWS 連携設定 (S3 などを利用する場合)
# -------------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# -------------------------------------------------------------
# Pusher (リアルタイム通知) 設定
# -------------------------------------------------------------
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

# -------------------------------------------------------------
# Google カレンダー連携設定
# -------------------------------------------------------------
# 現在から何日後まで同期するか
SYNC_MAX_DAYS=90

# 各 Google カレンダーの ID。不要なものは空にできます。
GOOGLE_CALENDAR_ID_PERSONAL=
GOOGLE_CALENDAR_ID_BUSINESS=
GOOGLE_CALENDAR_ID_SCHOOL=
GOOGLE_CALENDAR_ID_HOLIDAY=japanese__ja@holiday.calendar.google.com
# Google API 認証情報の JSON ファイルパス
GOOGLE_CALENDAR_PATH_TO_JSON=app/json/xxxxxxxxxxxxx.json

# Notion に取り込む際のラベル名
GOOGLE_CALENDAR_LABEL_PERSONAL=personal
GOOGLE_CALENDAR_LABEL_BUSINESS=work
GOOGLE_CALENDAR_LABEL_SCHOOL=school
GOOGLE_CALENDAR_LABEL_HOLIDAY=holiday

# -------------------------------------------------------------
# 通知関連 (メール / Slack)
# -------------------------------------------------------------
# 同期結果を送信するメールアドレス (複数の場合はカンマ区切り／送信不要なら空のまま)
SYNC_REPORT_MAIL_TO=
# Slack Bot 連携を有効にするかどうか
SLACK_BOT=false
# Slack Bot のトークン
SLACK_BOT_TOKEN=
# DM を送信するユーザー ID (カンマ区切り)
SLACK_DM_USER_IDS=

# -------------------------------------------------------------
# Notion API 設定
# -------------------------------------------------------------
# Notion API の統合トークン
NOTION_API_TOKEN=
# 同期先データベースの ID
NOTION_DATABASE_ID=database_id-of-your-database
# 利用する Notion API バージョン
NOTION_VERSION=2025-09-03
# データソース ID (インテグレーションとデータベースを紐付けた ID)
NOTION_DATA_SOURCE_ID=datasource_id-of-your-database

# -------------------------------------------------------------
# タイムゾーン設定
# -------------------------------------------------------------
TIMEZONE=Asia/Tokyo
